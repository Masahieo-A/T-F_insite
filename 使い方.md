# 校内陸上競技大会 記録管理Webアプリ 使い方

## 1. アクセス先

- 本番サイト: https://t-f-insite.vercel.app/
- データ保存先: Googleスプレッドシート「校内大会DB」
- アプリの保存状態は、記録入力画面上部に表示されます。

### 保存状態の意味

| 表示 | 状態 |
| --- | --- |
| DB同期済み | Googleスプレッドシートへの保存が完了しています |
| 同期中 | Googleスプレッドシートへ保存しています |
| 端末保存済み | 通信に失敗し、現在の端末内だけに保存されています |
| 同期済み | 画面を開いた直後などに一時的に表示されることがあります |

「端末保存済み」と表示された場合は、そのまま大会運営を続けず、通信状態を確認してから画面を再読み込みし、必要な入力を再度保存してください。

## 2. 基本的な運用順序

本大会はチーム発表と個人種目決めを当日に行います。次の順番で運用します。

1. 当日、A・B・Cのチームを生徒へ発表
2. 生徒が画面下部の「当日 種目登録」から自分の個人種目を登録
3. 管理者が「エントリ登録」→「選手種目登録」で登録状況を確認
4. 必要に応じて「大会管理」→「エントリー編集」で組・レーン・試順を調整
5. 「記録入力」から記録を入力
6. 入力確認後に「速報保存」
7. 管理者が確認し、「大会管理」→「競技管理」で状態を「確定」に変更

## 3. 競技一覧

サイトを開くと、競技一覧が表示されます。

- 「トラック」「跳躍」「投てき」で種目を絞り込めます。
- 「男子」「女子」で対象を絞り込めます。
- 組番号またはラウンドのリンクを押すと、結果一覧へ移動します。
- 「対抗戦集計」を押すと、チーム総合得点と種目別得点内訳を確認できます。
- 画面下部から「当日 種目登録」「記録入力」「エントリ登録」「大会管理」へ移動できます。

## 4. 生徒が当日に種目登録する

1. 競技一覧の一番下にある「当日 種目登録」を押します。
2. 発表された「Aチーム」「Bチーム」「Cチーム」から自分のチームを選びます。
3. 自分の氏名を選びます。
4. 「参加競技1～3」から出場する個人種目を選びます。
5. 出場しない枠は「未選択」にします。
6. 「入力内容を確認」を押します。
7. チーム、氏名、出場種目が正しいことを確認し、「この内容で登録」を押します。
8. 「○○さんの出場種目を保存しました」と表示され、保存状態が「DB同期済み」になったことを確認します。

注意事項:

- 個人種目は1人最大3種目です。
- 同じ種目を複数枠に重複して登録できません。
- 9×400m全員リレーは全員参加のため、個人のプルダウンでは選択しません。
- 登録を訂正する場合は、同じチーム・氏名を選び直して再登録します。
- 保存前に必ず自分の氏名であることを確認してください。

## 5. 結果一覧

結果一覧では、同じ種目の複数組を縦に並べて確認できます。

### 表示切替

- レーン順: レーン・試順の順に表示
- 順位順: 記録から自動計算した順位で表示
- 組別結果: 組ごとの結果
- 全体順位: 全組を合わせた順位
- 実力帯別: A・B・Cの実力帯ごとの順位

DNS、DNF、DQ、NMは順位計算から除外され、グレー行で表示されます。

## 6. 記録入力

### 6.1 入力画面を開く

1. 競技一覧の画面下部にある「記録入力」を押します。
2. 「入力種目」を選びます。
3. 「入力組」を選びます。
4. 選択した組の選手がレーン・試順で表示されます。

選手名は事前登録情報から表示されるため、記録入力画面では変更できません。

### 6.2 記録の入力方法

記録欄には数値だけを入力します。単位は自動で付与されます。

| 種目 | 入力例 | 表示例 |
| --- | --- | --- |
| 短いトラック種目 | `7.46` | 7.46秒 |
| 分を含むトラック種目 | `1:03.87` | 1:03.87 |
| 跳躍・投てき | `5.42` | 5m42相当 |

スマートフォンでは数字入力用キーパッドが表示されます。

### 6.3 DNS・DNF・DQ・NM

- DNS: 欠場
- DNF: 途中棄権
- DQ: 失格
- NM: 記録なし

該当するボタンを押すと選択状態になります。同じボタンをもう一度押すと取り消せます。別の状態ボタンを押すと選択が切り替わります。

### 6.4 入力確認と速報保存

1. 記録または状態を1件以上入力します。
2. 「入力内容を確認」を押します。
3. 「今回入力」と「保存済み」の内容を確認します。
4. 修正する場合は「入力へ戻る」を押します。
5. 問題がなければ「速報保存」を押します。
6. 画面上部が「DB同期済み」になったことを確認します。

速報保存すると、その種目の状態は自動的に「速報」になります。

## 7. エントリ登録

画面下部の「エントリ登録」には、次の3画面があります。

- 競技者登録
- 種目設定
- 選手種目登録

### 7.1 競技者登録

#### 個別追加

1. 選手Noを入力します。空欄の場合は自動採番されます。
2. 氏名を入力します。
3. 性別を選びます。
4. 「競技者を追加」を押します。

登録済み選手の氏名・性別は、一覧の各行で変更して「保存」を押します。

#### CSV一括登録

1. 「1. テンプレートをDL」を押します。
2. ダウンロードしたCSVをExcelやGoogleスプレッドシートで編集します。
3. CSV形式のまま保存します。
4. 「3. 編集済みCSVを取り込む」からファイルを選びます。
5. 取込完了メッセージと「DB同期済み」を確認します。

競技者CSVの列は次のとおりです。

`選手No, 氏名, カナ, 性別, 学年, チームID, 所属, 地域, 実力帯`

注意事項:

- 性別は「男子」または「女子」です。
- 学年は1～6の整数です。
- 実力帯は「A」「B」「C」のいずれかです。
- 選手Noを重複させないでください。
- CSVから選手行を削除して取り込むと、その選手のエントリーと記録も削除されます。
- テンプレートの見出し名は変更しないでください。

### 7.2 種目設定

1. 種目ごとに開始時刻を設定します。
2. 種目名を入力します。
3. 種類を「トラック」「跳躍」「投てき」から選びます。
4. 各行の「保存」を押します。

開始時刻は24時間表記の `HH:MM` 形式です。例: `13:50`

種目種類を変更すると、順位計算の方向と記録単位も切り替わります。

- トラック: 記録が小さい選手を上位として計算
- 跳躍・投てき: 記録が大きい選手を上位として計算

### 7.3 選手種目登録

1. 選手ごとの「参加競技1～3」を選びます。
2. 出場しない欄は「未選択」にします。
3. 入力後、「選手種目登録を保存」を押します。
4. 保存完了メッセージと「DB同期済み」を確認します。

プルダウンには「種目設定」に登録されている種目が表示されます。

注意事項:

- 1人につき最大3種目です。
- 同じ選手に同じ種目を重複登録できません。
- 新規エントリーの組は、現在の人数が少ない組へ自動配置されます。
- レーン・試順は、その組の末尾へ自動配置されます。
- 変更していない選手のエントリーは維持されます。

## 8. 大会管理

画面下部の「大会管理」には、次の機能があります。

### 8.1 競技管理

種目名、開始時刻、競技状態を管理します。

競技状態は次の5種類です。

- 編成済み
- 入力中
- 速報
- 確定
- 訂正中

種目名・開始時刻は編集後に行の「保存」を押します。状態はプルダウンで選択した時点で保存されます。

記録確認が完了した種目は「確定」に変更してください。「速報」または「確定」の種目がチーム得点の自動計算対象です。

### 8.2 選手登録

登録済み選手のNo、氏名、学年、性別、所属、実力帯を閲覧できます。

選手の追加・氏名変更・性別変更は「エントリ登録」→「競技者登録」で行います。

### 8.3 エントリー編集

#### CSV一括変更

1. 「1. テンプレートをDL」を押します。
2. CSVを編集します。
3. 「3. 編集済みCSVを取り込む」から読み込みます。

一括CSVの列は次のとおりです。

`種目ID, 種目名, 開始時刻, 組, レーン・試順, 選手No, 氏名, カナ, 学年, 性別, チームID, 所属, 地域, 実力帯, 得点対象`

注意事項:

- 種目IDは既存の値を使用し、変更しないでください。
- 同じ種目IDの種目名・開始時刻は、全行で同じ値にしてください。
- 組とレーン・試順の組合せを重複させないでください。
- 得点対象は `1`、対象外は `0` を入力します。
- 行を削除すると、その種目の該当出場枠も削除されます。
- 出場者を変更した枠では、変更前の選手の保存済み記録が削除されます。
- 取込前のCSVをバックアップとして残してください。

#### 個別変更

- 「個別変更する種目」を選びます。
- 各枠の出場者プルダウンから選手を変更できます。
- 「削除」で出場枠を削除できます。
- 「出場者を個別追加」で、組・レーン・試順・選手を指定して追加できます。
- レーン・試順を空欄にすると、その組の末尾番号が自動設定されます。

### 8.4 記録修正

1. 修正する種目を選びます。
2. 「訂正理由」を必ず入力します。
3. 訂正値を入力するか、状態をDNS・DNF・DQ・NMから選びます。
4. 「理由付きで訂正保存」を押します。

訂正保存後、種目状態は「訂正中」になります。内容を確認後、「競技管理」で「速報」または「確定」に戻してください。

### 8.5 監査ログ

次の操作履歴を確認できます。

- 競技情報変更
- 競技状態変更
- 速報保存
- 記録訂正
- CSV取込
- 選手・エントリーの追加、変更、削除

履歴には時刻、操作者、操作内容、対象、理由が表示されます。

## 9. 順位・得点の自動計算

順位と得点は手入力しません。

- トラック種目は記録の小さい順です。
- 跳躍・投てき種目は記録の大きい順です。
- 同記録は同順位になります。
- DNS、DNF、DQ、NMは順位対象外です。
- 実力帯別順位はA・B・Cごとに再計算されます。
- チーム得点とPBボーナスは登録済みルールから自動計算されます。
- チーム総合同点の場合は、1位数、次に2位数で順位を決定します。

## 10. Googleスプレッドシート

アプリの保存内容は「校内大会DB」へ反映されます。

主なシート:

- athletes: 選手
- events: 種目
- entries: エントリー
- results: 記録
- auditLogs: 操作履歴

`_state` という非表示シートには、アプリ全体を復元するためのデータが保存されています。

### 重要

- 通常の更新は必ずWebアプリから行ってください。
- Googleスプレッドシートのセルを直接書き換えても、Webアプリには反映されません。
- `_state` シートを表示・編集・削除しないでください。
- シート名を変更しないでください。

## 10. 大会当日の推奨体制

- 大会管理担当: 競技状態、エントリー、訂正、確定を担当
- 記録入力担当: 担当種目・組の記録入力と速報保存を担当
- 速報確認担当: 公開結果とチーム得点を確認

複数端末で同時に異なる操作をすると、最後に保存した端末の内容が優先される場合があります。エントリー変更、CSV取込、記録訂正などの管理操作は、原則として1台の管理端末で行ってください。

## 11. 運用上の注意

- 保存操作後は必ず「DB同期済み」を確認してください。
- CSV取込前には、現在のテンプレートをダウンロードしてバックアップしてください。
- 出場者変更・削除では、その枠の記録も削除されることがあります。
- 記録訂正には必ず理由を入力してください。
- ブラウザの戻るボタンではなく、画面上部の「TOP」ボタンを使用してください。
- 現在は管理者パスワード機能を設けていません。本番URLは大会運営関係者以外へ広く共有しないでください。

## 12. トラブル時

### DB同期済みにならない

1. インターネット接続を確認します。
2. 画面を再読み込みします。
3. 数秒待ち、「DB同期済み」になるか確認します。
4. 「端末保存済み」のままの場合は、新しい入力を続けず管理担当へ連絡します。

### CSVを取り込めない

1. ファイル形式がCSVか確認します。
2. 見出し行を変更していないか確認します。
3. 開始時刻が `HH:MM` 形式か確認します。
4. 選手No、組、レーン・試順に重複がないか確認します。
5. 画面に表示されたエラー行をCSVで修正し、再度取り込みます。

### 誤った記録を保存した

「大会管理」→「記録修正」で、訂正理由を入力して修正します。修正後に内容を確認し、競技状態を「確定」へ戻します。
